export interface I{{name}} {
    name: string;
    surname: string;
    middlename: string;
    birthdate: number;
}

export interface I{{name}}Store {
    user: IUser | null;
    set{{name}}: (user: I{{name}}) => void;
} 